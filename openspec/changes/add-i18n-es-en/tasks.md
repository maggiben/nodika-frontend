## 1. Routing and dictionaries

- [x] 1.1 Add locale config, `es`/`en` dictionaries, and dictionary loader/provider.
- [x] 1.2 Add `proxy.ts` locale redirects and move pages under `app/[locale]/`.
- [x] 1.3 Wire language switcher into the navbar and locale-aware links.

## 2. Localized copy

- [x] 2.1 Localize auth forms, dashboard, and upload UI strings from dictionaries.
- [x] 2.2 Sync OpenSpec specs and update route/component tests for `/es` defaults.

## 3. Validation

- [x] 3.1 Run format, lint, tests, coverage, change validation, and production build.
