## 1. Library and selection

- [x] 1.1 Replace single-snapshot storage with a multi-project library + selected id, including legacy migration.
- [x] 1.2 Update upload success to upsert/select and keep dashboard reading the selected project.

## 2. Navbar selector

- [x] 2.1 Add a navbar project selector that lists stored projects and updates the selected id.
- [x] 2.2 Keep selector visible whenever the local library has projects (signed in or out).

## 3. Specs and validation

- [x] 3.1 Sync OpenSpec deltas into main specs and update focused tests.
- [x] 3.2 Run format, lint, tests, coverage, change validation, and production build.
