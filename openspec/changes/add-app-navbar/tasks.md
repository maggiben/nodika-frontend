## 1. Shell and theme

- [x] 1.1 Wire `InitColorSchemeScript` and color-scheme-aware `AppTheme` for persisted light/dark/system modes.
- [x] 1.2 Add a shared `AppNavbar` with brand, unauthenticated auth actions, and authenticated avatar menu (theme + logout).
- [x] 1.3 Render the navbar from the root layout using the access-cookie session marker.

## 2. Page cleanup and tests

- [x] 2.1 Remove home-page-local session controls and keep a single main landmark/heading.
- [x] 2.2 Add/update component tests for navbar logout and theme preference actions.
- [x] 2.3 Sync accepted OpenSpec deltas into main specs and mark tasks complete.

## 3. Validation

- [x] 3.1 Run format, lint, tests, coverage, OpenSpec validation for this change, and production build.
