## 1. Theme and navigation

- [x] 1.1 Define distinct light/dark palettes in `AppTheme`.
- [x] 1.2 Add Upload snapshot to the authenticated avatar menu and link `/upload`.

## 2. Dashboard and upload route

- [x] 2.1 Add local snapshot persistence helper used after successful upload.
- [x] 2.2 Build the `/` dashboard (labels, graphs, task grid) from persisted snapshot JSON.
- [x] 2.3 Move upload UI to `/upload` and remove it from the home page.

## 3. Specs and validation

- [x] 3.1 Sync OpenSpec deltas into main specs and update focused tests.
- [x] 3.2 Run format, lint, tests, coverage, change validation, and production build.
