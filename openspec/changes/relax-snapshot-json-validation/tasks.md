## 1. Validation

- [x] 1.1 Replace schema validation in `src/lib/nodika-snapshot.ts` with JSONLint-style parse checks for objects.
- [x] 1.2 Update `src/lib/nodika-snapshot.test.ts` for syntax success/failure cases.

## 2. Upload surfaces

- [x] 2.1 Align form validation messaging with syntax-only checks.
- [x] 2.2 Confirm `POST /api/snapshots` uses the shared parser without schema rejection.

## 3. Validation

- [x] 3.1 Run format, lint, tests, coverage, OpenSpec validation, and production build.
